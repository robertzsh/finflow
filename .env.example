# Copy to ".env" for local dev, or set these as GitHub repo secrets for deployment.
# Leave blank / unset to run FinFlow in local-only mode (no accounts, browser storage).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
