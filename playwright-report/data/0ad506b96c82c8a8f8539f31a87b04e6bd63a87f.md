# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: 01-app.spec.ts >> App shell, navigation, routing, layout >> loads the dashboard with the 5 key stat cards and no console errors
- Location: tests/e2e/01-app.spec.ts:5:3

# Error details

```
Error: Dashboard never rendered. Open the HTML report and look at the screenshot: is it the login screen (cloud mode), a blank page (JS crash), or onboarding?

expect(locator).toBeVisible() failed

Locator: getByText(/account balance/i)
Expected: visible
Timeout: 8000ms
Error: element(s) not found

Call log:
  - Dashboard never rendered. Open the HTML report and look at the screenshot: is it the login screen (cloud mode), a blank page (JS crash), or onboarding? with timeout 8000ms
  - waiting for getByText(/account balance/i)

```

```yaml
- heading "Let's set you up" [level=1]
- paragraph: You can change these anytime in Settings.
- text: Your name
- textbox "Name": Robert
- text: Currency
- combobox:
  - option "lei RON — Leu românesc" [selected]
  - option "€ EUR — Euro"
  - option "$ USD — US Dollar"
  - option "£ GBP — British Pound"
- button "Enter FinFlow":
  - text: Enter FinFlow
  - img
- paragraph: Sample data is preloaded so you can explore right away.
```

# Test source

```ts
  1  | import { Page, expect } from '@playwright/test';
  2  | 
  3  | /** Open the app in LOCAL mode, complete first-run onboarding, and collect errors.
  4  |  *  Fails fast with a clear message if the build came up in CLOUD mode (login screen). */
  5  | export async function openApp(page: Page): Promise<string[]> {
  6  |   const errors: string[] = [];
  7  |   page.on('console', (m) => { if (m.type() === 'error') errors.push(m.text()); });
  8  |   page.on('pageerror', (e) => errors.push(String(e)));
  9  |   const ignore = (t: string) => /service ?worker|manifest|favicon|ERR_|Failed to load resource/i.test(t);
  10 | 
  11 |   await page.goto('/');
  12 | 
  13 |   // If a password field is on screen, the build is in CLOUD mode → E2E can't proceed.
  14 |   const loginVisible = await page.locator('input[type="password"]').first().isVisible({ timeout: 2500 }).catch(() => false);
  15 |   if (loginVisible) {
  16 |     throw new Error(
  17 |       'App is in CLOUD mode (login screen detected). E2E needs a LOCAL-mode build with NO ' +
  18 |       'Supabase env. Run: `VITE_SUPABASE_URL= VITE_SUPABASE_ANON_KEY= npm run build` (or use ' +
  19 |       '`.env.e2e` + `--mode e2e`). See E2E_TESTING.md.',
  20 |     );
  21 |   }
  22 | 
  23 |   // Local-mode first run shows a short onboarding — click through up to 4 steps.
  24 |   for (let i = 0; i < 4; i++) {
  25 |     if (await page.getByText(/account balance/i).first().isVisible({ timeout: 1200 }).catch(() => false)) break;
  26 |     const next = page.getByRole('button', { name: /continue|next|enter finflow|get started/i }).first();
  27 |     if (await next.isVisible({ timeout: 1000 }).catch(() => false)) { await next.click(); continue; }
  28 |     break;
  29 |   }
  30 | 
  31 |   await expect(
  32 |     page.getByText(/account balance/i),
  33 |     'Dashboard never rendered. Open the HTML report and look at the screenshot: is it the login screen (cloud mode), a blank page (JS crash), or onboarding?',
> 34 |   ).toBeVisible({ timeout: 8000 });
     |     ^ Error: Dashboard never rendered. Open the HTML report and look at the screenshot: is it the login screen (cloud mode), a blank page (JS crash), or onboarding?
  35 | 
  36 |   return errors.filter((e) => !ignore(e));
  37 | }
  38 | 
  39 | /** True if the page scrolls horizontally (a responsive red flag). */
  40 | export async function hasHorizontalScroll(page: Page): Promise<boolean> {
  41 |   return page.evaluate(() => document.documentElement.scrollWidth > document.documentElement.clientWidth + 1);
  42 | }
  43 | 
```